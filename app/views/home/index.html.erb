<p>Sesión iniciada, <%= current_user.email %></p>
<p><%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete %></p>

<%= render partial: 'slides/home_carousel', locals: { slides: @slides } %>
