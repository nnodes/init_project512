<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button id="toggle-navbar" type="button" class="navbar-toggle" data-toggle="<%= 'offcanvas' if user_signed_in?%>" data-target="#navbar-home-sidebar" data-canvas="body" data-user="<%=user_signed_in?%>">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <img class="logos" alt="Nnodes" src="<%= asset_path 'logo.svg' %>" height="20">
      </a>
    </div>
    <div class="collapse navbar-collapse hidden-sm hidden-xs" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", authenticated_root_path %></li>
        <% if current_user.admin? %>
          <li><%= link_to "Admin", admin_users_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-cog"></i><%= current_user.email %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%# link_to "Opciones",  %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Cambiar contraseña', edit_user_registration_path %></li>
            <li><%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
      <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Iniciar sesión", new_user_session_path %></li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>
